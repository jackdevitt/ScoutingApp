workflows:

    ios-workflow-id:
      name: iOS

      scripts:
        - name: Build for IOS
        script: flutter build iOS

      enviornment: 
        ios_signing:
          distribution_type: development
        # ......